[pytest]
markers =
    integration: Integration tests using real browser against mock HTML
    e2e: End-to-end tests requiring ProtonMail credentials and internet
asyncio_mode = strict
addopts = -m "not e2e"
